{
  "name": "@hypericon/yuzu",
  "version": "0.3.2",
  "description": "Library to push UI State updates across connected clients in CI applications",
  "main": "dist/index.js",
  "author": "Ed Johnson, Hypericon Ltd",
  "license": "UNLICENSED",
  "homepage": "https://github.com/hypcn/yuzu#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hypcn/yuzu.git"
  },
  "scripts": {
    "dev": "concurrently npm:dev-*",
    "dev-client": "rimraf example/client-out && parcel example/client/index.html -d example/client-out",
    "dev-server": "nodemon",
    "dev_old": "concurrently npm:dev_old-*",
    "dev_old-client": "rimraf example-old/client-out && parcel example-old/client/index.html -d example-old/client-out",
    "dev_old-server": "nodemon --config nodemon.old.ts",
    "build": "rimraf dist && tsc -p src/tsconfig.json",
    "watch": "rimraf dist && tsc -p src/tsconfig.json -w",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ws": "^7.4.3"
  },
  "peerDependencies": {
    "rxjs": "^6.6.3"
  },
  "devDependencies": {
    "@types/mithril": "^2.0.6",
    "@types/node": "^14.14.30",
    "@types/ws": "^7.4.0",
    "concurrently": "^5.3.0",
    "mithril": "^2.0.4",
    "nodemon": "^2.0.7",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.1.5"
  },
  "files": [
    "dist"
  ]
}
